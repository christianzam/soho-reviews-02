<section class="masthead" role="img" aria-label="Image Description" >
  <div class="welcome-text">
      <% if user_signed_in? %>
        <h1>Hello <%= current_user.username.capitalize %></h1>
      <% else %>
      <div class="welcome-text-1200">
        <h1>Welcome</h1>
        <h3>to</h3>
        <h2>SOHO reviews</h2>
      </div>
      <% end %>
  </div>
</section>
